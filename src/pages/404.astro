---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Button } from "@/components/ui/button";
---

<BaseLayout title="404 - 页面未找到">
  <div class="flex flex-col items-center justify-center min-h-[70vh] text-center px-4">
    <div class="relative">
      <h1 class="text-9xl font-bold text-primary/10 select-none">404</h1>
      <div class="absolute inset-0 flex flex-col items-center justify-center">
        <h2 class="text-3xl font-bold tracking-tight sm:text-4xl text-foreground">
          页面未找到
        </h2>
      </div>
    </div>
    
    <p class="mt-4 text-muted-foreground max-w-md mx-auto">
      抱歉，您访问的页面可能已经被移除、重命名或暂时不可用。
    </p>

    <div class="mt-8 flex gap-4">
      <Button id="go-back-btn" variant="outline" size="lg" className="rounded-full gap-2 px-8 shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-arrow-left h-4 w-4"><path d="m12 19-7-7 7-7"/><path d="M19 12H5"/></svg>
        返回上页
      </Button>
      <a href="/">
        <Button variant="outline" size="lg" className="rounded-full gap-2 px-8 shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-home h-4 w-4"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            返回首页
        </Button>
      </a>
    </div>

    <script>
      const backBtn = document.getElementById('go-back-btn');
      if (backBtn) {
        backBtn.addEventListener('click', () => {
          window.history.back();
        });
      }
    </script>
  </div>
</BaseLayout>
